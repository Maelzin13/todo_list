# PostgreSQL (Neon) - sobrescreva com env ou application-local.properties em produção
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-dry-silence-47372003-pooler.us-east-2.aws.neon.tech/minhasfinancas?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:Maelzin13}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ohYfzM68XbCk}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.default_schema=financas

# Flyway - migrations apenas para tasks (schema financas)
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.schemas=financas
# Schema já existe com tabelas (usuario, lancamento); baseline permite adicionar Flyway a schema existente
spring.flyway.baseline-on-migrate=true
